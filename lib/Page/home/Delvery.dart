import 'package:flutter/material.dart';

class Delvery extends StatelessWidget {
  const Delvery({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Delivery...'),
    );
  }
}
